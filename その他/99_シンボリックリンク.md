2. PowerShell を管理者権限で開く
3. 以下を実行（パスだけ変える）

cmd /c mklink /D "D:\02*作業\00*リンクワーク\HTML 自動化\56\_暗い縦書き最終テスト\学習" "D:\50_knowledge"

↑ 新プロジェクト の部分を実際のパスに変えるだけ
(学習フォルダは事前に作らない)

════════════════════════════════════════
【初回セットアップ】（1 回だけ）
════════════════════════════════════════

1. マスタフォルダを作る
   D:\50_knowledge
2. VS Code のワークスペースに追加
   ファイル → フォルダーをワークスペースに追加 → D:\50_knowledge を選択
3. GitHub に knowledge リポジトリを作る

GitHub で新規リポジトリ作成（名前：50_knowledge）

4. ターミナルで Git 設定
   powershellcd D:\50_knowledge
   git init
   git remote add origin https://github.com/あなたのユーザー名/50_knowledge.git
   git branch -M main
   git add .
   git commit -m "initial commit"
   git push -u origin main
   ※ 以降は VS Code の GUI で push/pull 可能

【新プロジェクト作成時】（毎回）

1. プロジェクトフォルダを作る
2. PowerShell を管理者権限で開いて実行
   powershellcmd /c mklink /D "プロジェクトのパス\学習" "D:\50_knowledge"

```

※ `学習` フォルダは事前に作らない

════════════════════════════════════════
## 【毎日の作業】
════════════════════════════════════════


**朝（会社 or 自宅に着いたら）**
```

1. プロジェクトを pull
2. knowledge を pull（VS Code のソース管理から）

```

**帰り（作業終わり）**
```

1. プロジェクトを push
2. knowledge を push（VS Code のソース管理から）

════════════════════════════════════════

## 最初会社でやること

════════════════════════════════════════

明日会社でやること

1. 会社で PJ フォルダを作成した場合、

・プロジェクトフォルダを作成
・リンク作成（PowerShell）
cmd /c mklink /J "プロジェクトのパス\学習" "C:\Users\guest04\Desktop\高橋研三\03_knowledge"
・.gitignore に「学習/」を追加
・　 GitHub でリポジトリ作成
・Git 紐付け（ターミナル）
git init
git remote add origin https://github.com/yoshida55/リポジトリ名.git
git branch -M main
git add .
git commit -m "initial commit"
git push -u origin main
・ワークスペースに 03_knowledge を追加
ファイル → フォルダーをワークスペースに追加
・ワークスペースを保存

---

（すでに自宅にあれば、）

1. knowledge を clone
   powershellcd C:\
   git clone https://github.com/yoshida55/50_knowledge.git

````
※ これでフォルダ作成とpullが同時に完了

**2. VS Codeのワークスペースに追加**```
ファイル → フォルダーをワークスペースに追加 → C:\50_knowledge
3. プロジェクトにシンボリックリンクを貼る
powershellcmd /c mklink /D "プロジェクトのパス\学習" "C:\50_knowledge"


※ポイント
clone = フォルダ作成 + pull が一緒にできる
マスターフォルダを手動で作る必要なし
cloneが先、リンクは後

2026-01-04-22-28-01.png


初回以降
毎朝 pull するだけ。cloneは最初の1回だけ。


※　ナレッジフォルダがワークスペースが同時に開かれないので、以下の設定も追加。

VS Codeの設定を変える
VS Codeが前回開いてたワークスペースを自動で開くようにする：

Ctrl + , で設定を開く
検索で window.restoreWindows と入力
folders に変更

これで次回VS Code起動時に、前回のワークスペースが自動で開く。


※　今後マークダウンがprettierで対象にならないように設定

方法1：VS Codeの設定で除外
Ctrl + , で設定を開いて、settings.json に追加：
json"[markdown]": {
  "editor.defaultFormatter": null,
  "editor.formatOnSave": false
}
```



【お客さんに納品するとき】
プロジェクト内の 学習/ フォルダ（リンク）を削除してから渡す。
※ マスタは消えないので安心
````
